import AdminLoaderVisual from "@/app/admin/component/AdminLoaderVisual";

/**
 * Shown while admin route segments load (RSC / server data).
 */
export default function AdminLoading() {
  return <AdminLoaderVisual variant="inline" />;
}
